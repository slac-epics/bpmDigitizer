# Do not execute directly.
# This file is part of the EPICS PCDS gateway system
# It expects iocenv.sh to be sourced and GATEWAY to be defined

# Re-define PV_PREFIX according to AMO naming conventions:
PV_PREFIX="NET:MCC:EGW:01"

# EBeam is using ports 5070 and 5071
CAS_PORT=5070

# In this configuration we only service one EPICS client: lcls-daemon3
export EPICS_CAS_BEACON_ADDR_LIST=lcls-daemon3
export EPICS_CAS_AUTO_BEACON_ADDR_LIST=NO
